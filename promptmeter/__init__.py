"""PromptMeter — local plan-window budgeting, risk scoring and prompt segmentation
for work you run through Claude.

Everything in this package is deterministic and offline. No API key, no network
calls, no model calls. The only external input is the status-line JSON that
Claude Code already produces on your own machine.
"""

__version__ = "1.0.0"
